pre-commit:
  parallel: true
  jobs:
    - name: typecheck
      run: bunx tsc --noEmit

    - name: lint
      glob: "*.{ts,json}"
      run: bunx biome check --no-errors-on-unmatched {staged_files}

    - name: test
      run: bun run test
