pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.go"
      run: golangci-lint run ./...

    - name: test
      glob: "*.go"
      run: go test ./... -race

    - name: build
      glob: "*.go"
      run: go build ./...
